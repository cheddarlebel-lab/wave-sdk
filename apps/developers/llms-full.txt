# Wave Unlock SDK — llms-full.txt

Wave Unlock lets a third-party app open a Wave Passport door over Bluetooth LE.
This file is the complete, flat reference for an AI builder or agent.

## Access & identity model (READ FIRST — this governs whether a door ever opens)

The SDK SENDS a credential; it does NOT make the access decision. The door controller does.

- A Wave credential resolves to a `cardID` (the canonical door identity).
- The controller (SICM) grants ONLY if that cardID exists in the site's PROVIDER SOURCE
  (the club's live membership system — MindBody, ABC Financial, Glofox, TwinOaks, etc.) or its
  synced PROVIDER CACHE (a local offline copy of the roster on the door hardware), AND the
  membership passes its rules (active schedule, dues/balance, check-in limits, restrictions).
- Therefore your app CANNOT grant access to a user who is not a member of that site.
  "Has your app installed" != "can open the door."

Two distinct identities you handle:
- Provider member id: what the user TYPES at enrollment (their id in the site's membership
  system — MindBody client id, barcode, etc.). Used ONCE, to locate the person in the roster.
- cardID: the door-truth key the controller validates on EVERY unlock. The on-door software
  resolves the typed member id -> cardID at enrollment and binds the Wave credential to it.
Enrolling a person who is not in the roster fails with `not_found` and issues NO credential.

Two ways to make your users door-eligible:
1. They are already members of the site's provider (DEFAULT). They enroll with the member id
   they already have; the device resolves it to their cardID; nothing to provision.
2. Passport is the provider for the site. When there is no third-party membership system, the
   site can run a Passport-hosted roster (managed in the Passport dashboard). You provision your
   users into that roster first (an operator/site setup step arranged with Passport, NOT a
   runtime SDK call); the controller syncs them; then credentials resolve normally.

Building for a site you do not operate? Confirm with the operator/Passport which provider the
door uses and that your users exist in it BEFORE you ship.

## Mental model

The unlock is a 6-step pipeline; your app owns steps 1-3 and rendering step 6. Steps 4-5 happen
on the door hardware and the provider — which is why a valid token can still be denied.
1. Scan for the reader — BLE name prefix "SKBluTag" OR service UUID
   496B2C43-B05E-4A9A-9592-535173B7AB51.
2. Proximity gate — compare RSSI to the site threshold (default -65, tunable per site from the cloud).
3. Write the credential — characteristic 995B637F-13F2-4335-96F5-5541ECFCE219,
   write WITHOUT response, payload = one byte 0x01 followed by the member's userNumber as ASCII.
   IMPORTANT: treat the write as immediate success; do NOT wait for a BLE notify (it deadlocks the reader).
   Disconnect ~1.5s after writing.
4. Resolve — the on-door acceptor verifies the one-time token and hands the member's real cardID
   to the controller (SICM).
5. Decide — the controller validates that cardID against the site's provider source/cache and its
   membership rules, then grants or denies.
6. Await the cloud result (poll unlock-stream up to 5s) and present it: "granted", or a friendly
   denial reason.

## Keys

- wave_pub_<32 hex>  Publishable. Safe to embed in an app. Exchanges for a LIVE session token.
- wave_test_<32 hex> Test. Exchanges for a TEST session token. Only test tokens may drive the mock.
- wave_sk_<48 hex>   Secret. Server-side only. NEVER embed in an app. Used to provision partners.

Session tokens are short-lived (300s) and scoped to the partner's allowed sites.

## Gateway HTTP API

Base URL: https://app.wavepassport.com/api
No API key header is required — the branded gateway injects it server-side.

POST /partner-auth/register            (header x-wave-admin-key: <admin key>)
  body { name, allowed_sites: string[] }
  -> { partner_id, publishable_key, secret_key, test_key }   (keys shown ONCE)

POST /partner-auth/token
  body { key: "wave_pub_*" | "wave_test_*" }
  -> { token, mode: "live"|"test", expires_in: 300 }

POST /unlock-stream                    (header Authorization: Bearer <session token>)
  body { card_id, binding_id? }
  -> { status: "granted"|"denied"|"pending", reason: string|null }
  Scoped to the caller's allowed sites — you only ever see your own members' events.

POST /unlock-mock                      (header Authorization: Bearer <TEST session token>)
  body { card_id, scenario }
  -> emits a synthetic unlock event. Rejected (403) for live-mode tokens.
  Scenarios: granted, member_not_found, outside_hours, membership_expired, outstanding_balance.

## Denial reasons (SICM -> friendly)

Granted by provider cache -> Access Granted
Client not found -> Member not found
Blocked by schedule -> Outside access hours
Maximum active signins -> Maximum active sign-ins reached
Checkins limitation -> Daily check-in limit reached
Blocked by restriction -> Membership restriction
Blocked by client alert -> Account alert
Over account balance -> Outstanding balance
Liability release -> Liability release required
Scheduled visit -> No booking found
No member picture -> Photo required
Provider not found -> System error - provider unavailable
Inactive -> Membership inactive
Expired -> Membership expired

## Testing with no hardware

Exchange a wave_test_* key for a token, POST the scenario to /unlock-mock, then read
/unlock-stream — you get the exact grant/deny path a real door would produce. The MCP
tool wave_simulate_unlock does all three in one call.

## MCP server (@wave/mcp) — the primary integration channel

Add to your agent (Claude Code / Cursor):
  { "mcpServers": { "wave": { "command": "npx", "args": ["-y", "@wave/mcp"],
      "env": { "WAVE_GATEWAY_URL": "https://app.wavepassport.com/api" } } } }
  (No Supabase/anon key: the SDK only ever talks to the branded gateway, which injects its own
   backend key server-side. Partner keys are passed to the individual tools, never as env secrets.)

Tools: wave_docs, wave_validate_config, wave_doctor, wave_scaffold, wave_register_app, wave_simulate_unlock.

## Platform support

- iOS (Swift), Android (Kotlin): full background BLE. Native packages (SPM / Maven).
- React Native, Flutter: thin bridges over the native cores.
- Web: Web Bluetooth, foreground-only (Chrome/Edge, secure context). Runnable today via the gateway.

## Integration-time checklist (what wave_doctor checks)

- iOS: Info.plist NSBluetoothAlwaysUsageDescription; UIBackgroundModes includes bluetooth-central.
- Android: BLUETOOTH_SCAN and BLUETOOTH_CONNECT permissions in AndroidManifest.xml.
- Config: publishable key (not secret) embedded; sites are non-empty strings.
