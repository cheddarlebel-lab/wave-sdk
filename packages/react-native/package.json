{
  "name": "@wave/unlock-react-native",
  "version": "0.1.0",
  "description": "Wave Passport BLE door unlock for React Native — thin bridge over the native Swift/Kotlin cores.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "ios", "android"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "peerDependencies": {
    "react-native": ">=0.72.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
