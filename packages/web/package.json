{
  "name": "@wave/unlock-web",
  "version": "0.1.0",
  "description": "Wave Passport BLE door unlock for the web (Web Bluetooth, foreground-only).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.20",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
